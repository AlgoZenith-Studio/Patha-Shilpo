{
  "name": "pathashilpa-functions",
  "description": "Cloud Functions for Pathashilpa",
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.0.2",
    "firebase-functions": "^7.3.2"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  },
  "private": true
}
