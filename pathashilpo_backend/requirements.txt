# Pinned to the versions this project is actually developed and tested against
# (Python 3.12, 37/37 backend tests green).
#
# These were open-ended `>=` with no upper bound, which made the Render build
# fail with `resolution-too-deep`: pip had to explore hundreds of version
# combinations and gave up. Exact pins make the resolve trivial, the build
# fast, and — most importantly — make production match what was tested.
#
# The Dockerfile pins python:3.12-slim. If this is ever built OUTSIDE Docker,
# pin the interpreter too (Render: set PYTHON_VERSION=3.12.7), because several
# of these have no wheels for 3.14 yet and pip will backtrack again.

fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
pydantic-settings==2.15.0

# Firebase Admin
firebase-admin==7.5.0

# AI services
google-generativeai==0.8.6       # Gemini
httpx==0.28.1                    # Bhashini, Sarvam, fal.ai HTTP calls
fal-client==1.0.1                # fal.ai SDK

# Utilities
python-multipart==0.0.32         # file uploads
python-jose[cryptography]==3.5.0 # JWT verification
Pillow==12.3.0                   # server-side image processing
